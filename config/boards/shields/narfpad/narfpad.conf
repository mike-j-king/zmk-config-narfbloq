# Shield-level config for Narfpad PCB
# These are hardware features of the macropad itself

# Rotary encoder (connected to narfpad PCB)
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# NOTE: USB and Bluetooth settings are in config/xiao_ble.conf (board-level)
